# fuzzy camera

a camera app that turns pixels into black or white randomly depending on how light they are (white are always white, black are always black, grays are 50/50 black/white)

## running

```
poetry install --no-root
poetry run python fuzzy_camera.py
```

## stopping

ctrl + C
